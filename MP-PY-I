import matplotlib.pyplot as plt
import numpy as np
n = np.arange(0,100)
c = 0
y = np.array([])
while (c < 100):
  
    if (c<9):
        z = (c*c)-7
        y = np.insert(y,c,z)
        c = c + 1
    else:
        z = ((c-10)**2)-7
        y = np.insert(y,c,z)
        c = c + 1
plt.plot(n,y)
