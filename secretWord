def isWordGuessed(secretWord,lettersGuessed):
    ctr=0
    for x in str(secretWord):
        y = x in lettersGuessed
        ctr=ctr+1
        if y == False:
            print('False')
            break
            
        elif ctr == len(secretWord) and y == True:
            print('True')
